// https://leetcode.com/problems/distant-barcodes/
#include <iostream>
#include <vector>
#include <map>
using namespace std;

vector<int> mix(vector<int> v) {
  // store to map
  // use priority que
  // return new vector

  return {1};
}

int main()
{
  vector<int> first = mix({1,1,1,2,2,2});
  vector<int> seconds = mix({1,1,1,1,2,2,3,3});
  return 1;
}
